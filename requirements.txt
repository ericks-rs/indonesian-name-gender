# Versions the reported results were produced under are given beside each floor.
# The floors are what the code needs, the pins are what it was run with.

torch>=1.12          # tested 2.8.0+cu128, norm_first arrived in 1.12
numpy>=1.21          # tested 1.23.5
pandas>=1.3          # tested 1.5.3
scikit-learn>=1.0    # tested 1.2.1
scipy>=1.7           # tested 1.10.0
matplotlib>=3.5      # tested 3.7.0, figures only
